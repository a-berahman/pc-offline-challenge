package common

const (
	//TranlatorService returns enum value for service collection
	TranlatorService = iota
)

const (
	//CacheRepo returns repo enum value for repository repository collection
	CacheRepo = iota
)
